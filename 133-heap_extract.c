#include "max_binary_heap.h"

/**
 * heap_extract - extracts the root node of Max Binary Heap
 * @root: double pointer to the root node of heap
 *
 * Return: Either the value stored in root node or 0 on failure.
*/
int heap_extract(heap_t **root)
{
	(void)root;

	return (0);
}
